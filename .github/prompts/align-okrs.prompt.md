# Align OKRs

Read `ai-workflows/prompts/align_okrs.md`, `sample-data/okrs.csv`, and the generated planning
outputs. Write `outputs/ai_okr_alignment.json` plus `outputs/ai_okr_alignment.md`. Treat the result
as an alignment draft, not approved OKRs.
