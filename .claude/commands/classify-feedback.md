---
description: Classify the notes in input-notes/ into structured product feedback
---

Follow `ai-workflows/prompts/classify_feedback.md`. Read the notes in `input-notes/` (or the file
named in $ARGUMENTS), classify them, and write `outputs/ai_feedback_classification.json` plus a
readable `outputs/ai_feedback_classification.md`. Use only the evidence in the notes; do not invent
facts.
